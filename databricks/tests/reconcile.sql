-- Placeholder: replaced by agents/tools/generate_from_inventory.py
-- (databricks/generated/reconcile.sql → _rendered/tests/reconcile.sql).
SELECT 'reconcile_placeholder' AS check_name,
       'Run generate_from_inventory.py before make run' AS message;
