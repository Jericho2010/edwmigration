# Gold convert outputs

Run artifacts written by **edw-convert** (`databricks/gold/<nn>_<slug>.sql`).

Not committed. The medallion job skeleton does not run anything from this directory until `check_job_wiring.py --apply` wires paths for the current backlog.

Do **not** copy `demo/wwi/reference/` here.
